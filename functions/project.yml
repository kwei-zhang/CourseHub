packages:
  - name: announcements
    functions:
      - name: send
        runtime: nodejs:18
        web: true
        environment:
          RESEND_API_KEY: ${RESEND_API_KEY}
          FROM_EMAIL: ${FROM_EMAIL}
