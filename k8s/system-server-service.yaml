apiVersion: v1
kind: Service
metadata:
  name: system-server
  namespace: lrms
spec:
  selector:
    app: system-server
  ports:
    - protocol: TCP
      port: 5003
      targetPort: 5003
  type: ClusterIP